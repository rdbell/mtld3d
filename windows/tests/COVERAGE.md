# mtld3d end-to-end test coverage

Every test is one isolated `#[test]` driving the real `d3d9.dll` through the
shared [`Harness`](src/harness.rs), verified by pixel readback, `HRESULT`, or a
getter round-trip — no manual inspection. Run with `make test` (nextest runs
each test in its own Wine process, in parallel, on both `i686`/`x86_64`
windows-msvc; the host-native `mtld3d-core`/`mtld3d-shared` unit tests run too).

A test that needs a non-default option sets it itself: because each test is its
own process and config resolves lazily on the first `Direct3DCreate9`, the test
appends its key to `MTLD3D_CONFIG` before constructing the `Harness`. That keeps
the option-gated behaviour in the ordinary `make test` run rather than behind a
command a reader has to be told about. Eight options are gated this way today:
`buffer.ignoreLockBounds` in `buffers.rs`, `depth.aliasSameSize` in
`render_target.rs`, `intel.expandPacked16` in `expand16.rs`,
`intel.denyFloat32Filtering` in `float_filter.rs`, `intel.managedMemory` and
`intel.linearAlign256` in `non_uma.rs`, `render.scale` in `render_scale.rs` and
`implicit_surface.rs`, for the quantities that convert between the reported and
rasterized spaces instead of staying in one of them, and `cursor.software` in
`device.rs`.

The whole suite also runs under every `intel.*` key at once with
`make test INTEL=1`, the way it would on an Intel/AMD Mac, for a machine
without Intel hardware. A test that asserts a capability answer asks the
device first and asserts the answer it gets, so it holds under either variant
and on real Intel hardware.

## Covered behaviour by file

`make audit` cross-checks this table against the files: every
`windows/tests/tests/*.rs` needs a row, and every row needs its file. A new
test file lands with its row in the same change.

| File | Coverage |
| --- | --- |
| `smoke.rs` | Clear-to-colour fill; `DrawPrimitiveUP` triangle with interpolated diffuse. |
| `device.rs` | `Direct3DCreate9`; adapter count/identifier/display-mode; `GetAdapterModeCount`/`EnumAdapterModes` (valid + out-of-range; the main module's own `EnumDisplaySettingsW` import enumerating only sizes `EnumAdapterModes` serves, within the bound, with `ENUM_CURRENT_SETTINGS` untouched); `CheckDeviceType`/`CheckDeviceFormat`/`CheckDeviceFormatConversion` (accept + reject; the conversion table incl. R5G6B5 and packed YUV sources, and windowed `CheckDeviceType` following the conversion predicate; the wide-channel family, 16-bit unorm and the floats, advertised as texture, cube and render target and creatable as each; the reversed-channel pair A8B8G8R8/X8B8G8R8 and 24-bit R8G8B8 advertised as texture, cube and sRGB-decoding sampled formats and creatable as each, with the render-target and `CreateRenderTarget` answers split between the two, native for the reversed-channel pair and denied for the format the upload pass widens; the per-resource-type usage rule, where the sampling-only query bits `QUERY_FILTER`/`QUERY_SRGBREAD`/`QUERY_VERTEXTEXTURE`/`QUERY_WRAPANDMIP`/`DYNAMIC` reject on a plain `D3DRTYPE_SURFACE` while answering on `D3DRTYPE_TEXTURE`, and the binding, blend and render-target sRGB-encode answers a surface does express stay); `GetDeviceCaps` sanity (SM2 sub-structs at the ps_2_0 floor, cube/volume filter and address caps, no VTF and `QUERY_VERTEXTEXTURE` rejected to match); `TestCooperativeLevel`; `Reset` (0×0 reject, same-size state-default restore, resize, fullscreen monitor-rect + style adoption and restore, `D3DCREATE_NOWINDOWCHANGES` leaving the device window's style, rect and visibility alone across the fullscreen round trip, a fullscreen Reset or create at a settable mode setting that display mode so `EnumDisplaySettingsW`, `GetSystemMetrics`, the client rect and the back buffer all read the mode, the registry mode coming back on the windowed Reset and on release, the deactivation restore and the activation re-set through synthesized `WM_ACTIVATEAPP`, a non-mode request following the window instead, undrawn post-resize `Present` reads back black, and the per-stage cube binding mask cleared with the slots, so the first draw after a `Reset` sees no stage typed as a cube and the stage the cube held samples a 2D texture as 2D); a device created and rendered after a previous one was destroyed, pinning that teardown drops the unix-side display latches and the next device's attach re-establishes them; the software cursor (`cursor.software=true`) realizing a blank HCURSOR on `ShowCursor(TRUE)` that `ShowCursor(FALSE)` leaves in place instead of pushing null, and presenting frames unchanged across sprite uploads and show/hide. |
| `draw.rs` | XYZRHW screen-space quad; every accepted primitive type (point/line/linestrip/tristrip); triangle fans through `DrawPrimitiveUP` (including a 300 triangle fan that outgrows the encoder's shared index pattern mid-frame), `DrawPrimitive` (bound stream, `StartVertex`, and the same mid-frame pattern growth) and `DrawIndexedPrimitive` (bound 16-bit indices, `StartIndex` + `BaseVertexIndex`, and two fans in one `D3DUSAGE_WRITEONLY` `D3DPOOL_DEFAULT` index buffer whose CPU copy was released, so the indices are read back off the GPU and the second fan is written through a later announced window), all rewritten as triangle lists; `DrawIndexedPrimitiveUP`; `ProcessVertices` transforming through the current FVF, and rejected from an explicit declaration. |
| `points.rs` | `D3DRS_POINTSIZE` sizing the square; an XYZ-only point under an ortho projection (Wine's test_pointsize shape); several sizes in one scene with a Clear between scenes and no Present; `POINTSIZE_MIN`/`MAX` clamp; `D3DFVF_PSIZE` per-vertex size; `POINTSCALEENABLE` with the viewport-height factor and eye-distance attenuation; programmable VS `oPts` vs the render-state default; point sprites through fixed-function and `ps_2_0` texcoords (quadrant texture), and the sprite state leaving triangles alone. The probes one pixel either side of a square's edge, and the one-pixel points whose every probe is a boundary, assert at the default `render.scale` alone; every extent with room around it is asserted at any scale. |
| `clip_planes.rs` | Fixed-function plane keeping the positive side and released when disabled; world-space semantics under a translated view; `D3DRS_CLIPPING` gating; two sparse-index planes intersecting; RHW geometry ignoring the planes; clip-space semantics under a programmable VS; `D3DSBT_ALL` capture/apply round trip that renders. |
| `buffers.rs` | `CreateVertexBuffer`/`CreateIndexBuffer`; unaligned `Lock` output slots on both buffer types, including successful locks, invalid-range null outputs and untouched neighboring bytes; `DrawPrimitive`/`DrawIndexedPrimitive` from bound streams; DYNAMIC+DISCARD refill; `GetDesc` round-trips; `GetStreamSource`/`GetIndices` round-trips including higher streams and the NULL-bind offset/stride retention; under `buffer.ignoreLockBounds=true`, a `Staged` (DEFAULT, non-DYNAMIC) vertex buffer and index buffer each carrying the writes a title makes past the `[OffsetToLock, SizeToLock)` window it announced, checked by re-locking a window whose announced bytes do not change and reading back the pixel the rest of the write moves; the CPU-backing release a `D3DPOOL_DEFAULT` `D3DUSAGE_WRITEONLY` non-`DYNAMIC` vertex buffer takes after an upload has carried every byte, checked by refilling half of a two-triangle buffer through an announced sub-range and through a zero-`SizeToLock` tail and reading back the half neither `Lock` named, and by a `Reset` after a release still leaving buffer draws working. |
| `streams.rs` | A two-stream declaration through a programmable VS; a zero `SetStreamSource` stride feeding every vertex the element at the stream offset; the `SetStreamSourceFreq` contract (defaults, rejections leaving state untouched, flag round-trip); instanced indexed draws (count from stream 0, per-instance step rate, non-indexed draws never instance, no per-instance stream means one instance); recorded and `D3DSBT_ALL` state blocks restoring stream bindings and frequencies. |
| `render_states.rs` | Alpha + additive blend; COLORWRITEENABLE mask; scissor; cull-mode winding; defaults vs `render_state_defaults()`; set/get round-trip; stencil round-trip; stencil test gating a draw; stencil clear preserving depth; combined depth+stencil mid-frame clear resetting both planes; stencil reference compared through the mask; stencil clear and test against a depth-only surface; wireframe no-op (pinned).; a Clear after a draw in the same pass (clear quad) surviving the draw's cull mode; `D3DRS_SRGBWRITEENABLE` encoding an opaque draw's linear 0x80 to 0xBC in a render-target texture, and blending in linear before that encode (half-alpha white over a stored 0x80 lands on 0xCD, not the 0xC0 a gamma-space blend gives) on a render-target texture, on the back buffer, and on a `CreateRenderTarget` surface. |
| `textures.rs` | Lock/sample A8R8G8B8/X8R8G8B8/R5G6B5/A1R5G5B5/X1R5G5B5/A4R4G4B4/L8 (X1R5G5B5 with its padding bit clear, which also blends as fully opaque under SRCALPHA and round-trips a texel through a 5-5-5 `GetDC` DIB); a 2x2 lock/sample round trip per quadrant for the reversed-channel A8B8G8R8/X8B8G8R8 pair and for 24-bit R8G8B8, whose three-byte texels stride an eight-byte row and are widened into the BGRA8 backing by the GPU upload pass, plus X8B8G8R8 sampling alpha as 1 through a SRC_ALPHA-blended draw whatever its padding byte holds; DXT1 block decode; mip chain levels/dims; every level of an 8x8 mip chain carrying its own texels, which puts the bottom two mips under the linear texture alignment and so on the GPU upload pass; AUTOGENMIPMAP (level count, `GetLevelDesc` usage, filter-type round trip, and `UnlockRect` rejecting a level the chain does not expose); SetLOD no-op; cube creation in all pools; CPU-only extension-format cubes; managed DXT face isolation; cube face upload, sampling, state blocks, render targets, and AUTOGENMIPMAP (including `GetCubeMapSurface` rejecting a level the chain does not expose); volume creates; `UpdateTexture` between volumes copies every slice (sampled per slice centre through the fixed-function 3D texcoord path); a level from `GetVolumeLevel` locked through `IDirect3DVolume9` and written samples back on every slice; the `D3DPOOL_DEFAULT` lock split (`default_pool_texture_lock_splits_by_entry_point`: a non-DYNAMIC level rejected through `IDirect3DSurface9::LockRect` and served through `IDirect3DTexture9::LockRect`, both entry points serving it once the texture carries `D3DUSAGE_DYNAMIC`); `GetDC` on level 0 of a 33x4 R5G6B5 managed texture, whose odd row length is where the level's staging and the stride a DIB steps by would part company (the DC reads the last texel of the last row, GDI's drawing reaches the staging at `ReleaseDC`, and a draw after it samples what GDI drew); `GetDC` and `LockRect` on one texture level excluding each other in both orders, each accepted once the other side is given up (the level's lock lives on the parent texture, out of reach of the surface shell's own map count); the same exclusion read across the whole resource, where `GetDC` on level 0 is rejected while level 1 is locked and `IDirect3DTexture9::LockRect` on either level is rejected while a level surface holds a DC, both accepted again once the other side is given up; `UpdateTexture` from a transposed source (2x4 into 4x2, which D3D9 accepts because the two agree on their larger dimension) copies the 2x2 the levels share and leaves the rest; the system-memory pool contract (a `D3DPOOL_SYSTEMMEM` texture, which allocates nothing on the GPU until it is bound, samples what was written to it once bound and picks up later writes; `LockRect` round-trips its texels; a level and an offscreen surface of one odd-width 16-bit size report one `LockRect` pitch, with rows written at it surviving `UpdateTexture` and sampling; the texels survive a resizing `Reset` and still reach the GPU through `UpdateTexture` afterwards; and no `D3DPOOL_SYSTEMMEM` / `D3DPOOL_SCRATCH` surface is accepted by `SetRenderTarget`); a block-compressed `D3DPOOL_SCRATCH` volume, which only the CPU-only path can represent, created, locked and written with the GPU pools rejecting the format; a DEFAULT-pool level written through four partial `UpdateTexture` rects that together cover it (its staging is released only once they do) sampling back whole, and a fifth partial update after that release leaving the quadrants it does not touch alone; a `LockRect` of a DEFAULT-pool level whose staging was released after its upload reading the level's own texels back (and the `D3DLOCK_DISCARD` form rewriting it whole from fresh pages).a sub-rectangle `UpdateSurface` from a system-memory offscreen-plain source into a default-pool level whose staging was already released, leaving the texels outside the rectangle untouched;a released DEFAULT-pool level reading the level's own texels back through the DIB, with GDI's `SetPixel` on it sampling back after `ReleaseDC`; a `GetDC` held across two draws that upload the level keeping its staging, so GDI's `SetPixel` under the held DC still samples back after `ReleaseDC`; `GetDC` onthe same `UpdateSurface` between two draws of one scene reaching the second draw, with no other state change to dirty the snapshot for it;the same standalone-source `UpdateSurface` rejecting a managed destination, a destination of another format and a `D3DPOOL_SCRATCH` source, and accepted for the matching default-pool destination and system-memory source;; `UpdateTexture` rejecting a resource-type mismatch in both directions (2D into volume, volume into 2D) with the destination left as it was, while a volume pair one depth slice deep, which is backed 2D, is still accepted; `UpdateTexture` accepting a source that still has one of its levels mapped, unlike the per-endpoint rejection `UpdateSurface` applies (pinned).; a single-slice DEFAULT volume taking a second `UpdateTexture` after the first one has been drawn, the point a droppable level's staging would have gone; `UpdateSurface` and `UpdateTexture` across a format pair the CPU codec covers (X8R8G8B8 into A8R8G8B8, R5G6B5 into X8R8G8B8, A8R8G8B8 into R5G6B5, A1R5G5B5 into A8R8G8B8, A8R8G8B8 into A4R4G4B4, L8 into A8R8G8B8, R8G8B8 into A8R8G8B8, A8B8G8R8 into A8R8G8B8, each sampled back), with a block-compressed source still answering `D3DERR_INVALIDCALL`., converting an X8R8G8B8 source into an A8R8G8B8 level and into a cube face, and still rejecting the pairs the codec omits (V8U8 into R5G6B5, any source into a DXT1 destination); a `Levels` count one past the chain the extent allows resolving to that chain on all four create paths (`CreateTexture`, the depth-format `CreateTexture`, `CreateCubeTexture` and `CreateVolumeTexture`, the last measured on its depth as well), with the count at exactly the chain unchanged; the lock contract a level's contents rest on (`dynamic_whole_level_lock_without_discard_keeps_the_texels_it_does_not_rewrite`, `partial_discard_lock_leaves_the_rest_of_the_level_alone`, `discard_lock_of_a_managed_level_is_ignored`): a DEFAULT DYNAMIC level locked whole without `D3DLOCK_DISCARD` while its previous upload is still in flight, rewritten in one quadrant and sampled back with the other three intact; the same with a `D3DLOCK_DISCARD` sub-rect lock, which cannot be honoured and is served as a plain partial lock; and a MANAGED level whose whole-level `D3DLOCK_DISCARD` is ignored the same way; the rect a lock names being what its `UnlockRect` publishes (`partial_locks_publish_their_own_rect_on_every_upload_path`): four contended quadrant locks on a DEFAULT DYNAMIC A8R8G8B8 level, on a DXT1 level with block-aligned rects and with rects off the block grid (the encoder's whole-mip fallback), and on one face of a cube, each sampled back as its four quadrants |
| `expand16.rs` | The packed 16-bit expansion path under `intel.expandPacked16=true` (the non-Apple-family behaviour, forced so it runs on Apple Silicon): A4R4G4B4/R5G6B5/A1R5G5B5/X1R5G5B5 lock/sample through the BGRA8 backing, with X1R5G5B5's padding bit still widening to an opaque alpha; an R5G6B5 mip chain sampled at every level, whose low mips have row pitches under the linear texture alignment; a partial lock re-uploading only the dirty sub-rect (2 bpp → 4 bpp widening with the border preserved); an A4R4G4B4 cube face; an R5G6B5 volume sampled per slice; a DEFAULT offscreen-plain R5G6B5 surface as a scaling `StretchRect` source; and the caps split: `CheckDeviceFormat(RENDERTARGET)` denied for R5G6B5/A1R5G5B5 with `CreateRenderTarget`/`CreateTexture(RENDERTARGET)` rejected to match, while the sampled-texture (X1R5G5B5 included), conversion-source, and windowed 16-bit-backbuffer answers stay advertised, and X1R5G5B5's render-target answer is denied on every device. |
| `float_filter.rs` | The 32-bit float filtering caps split under `intel.denyFloat32Filtering=true` (the answer a GPU without Metal's 32-bit float filtering gives, forced so it runs on Apple Silicon): `CheckDeviceFormat(D3DUSAGE_QUERY_FILTER)` denied for R32F/G32R32F/A32B32G32R32F, alone and combined with `D3DUSAGE_RENDERTARGET`, while their texture and render-target answers stay advertised and both creates still succeed; the half-float members and the 8- and 16-bit unorm formats keep filtering. |
| `non_uma.rs` | The storage policy of a GPU without unified memory under `intel.managedMemory=true` and `intel.linearAlign256=true` (Managed CPU-visible buffers with `didModifyRange` after every write, and the 256-byte linear texture alignment, forced so they run on Apple Silicon): a Managed vertex and index buffer drawing an indexed quad; a dynamic Managed buffer refilled with `D3DLOCK_DISCARD` across frames and between two draws of one frame; a 32x32 A8R8G8B8 and X8R8G8B8 mip chain, every pitch under the floor, sampling its own texels at every level through the upload pass and the padded-staging repack; and a 16x16 lockable render target written through `LockRect` at the padded stride and read back through `GetRenderTargetData`. On unified memory a `didModifyRange` that was left out is invisible, so these pin that the paths run and render, not that every write is notified; the `GpuCaps` unit tests pin that the keys reach the snapshot. |
| `subresource_identity.rs` | Sub-resource identity: `GetSurfaceLevel`, `GetCubeMapSurface` and `GetVolumeLevel` each return one cached object per sub-resource (same pointer twice, distinct objects for distinct levels and faces, the object surviving its own refcount reaching zero and still describable); private data set through one handle read back through another, on a level surface and on a volume level; both handles released leaving the container's refcount where it started; and a level surface handed to `SetRenderTarget` outliving the application's release of the surface AND its container, still answering `GetDesc`/`GetDevice` through it. |
| `samplers.rs` | State round-trip; CLAMP≠WRAP past the unit square; POINT≠LINEAR; BORDER with a white border colour reads white; a non-preset border colour falls back to opaque black (pinned Metal limitation); `D3DSAMP_SRGBTEXTURE=1` decodes sRGB→linear, including on a mid-scene flip over an unchanged bind, and the X8R8G8B8 twin view keeps the alpha=1 swizzle; `D3DSAMP_MIPMAPLODBIAS` shifting which level of a mip-tinted texture a one-texel-per-pixel quad samples, through the fixed-function cascade and through a `ps_3_0` `texld`. |
| `implicit_surface.rs` | The device-owned implicit surfaces: `GetRenderTarget(0)` hands back one cached object (the same pointer every call) and it is the same object as `GetBackBuffer(0)`; that object outlives every reference the application holds on it (device-owned, torn down with the device) and still answers `GetDesc` with the current backbuffer extent; its identity survives a resizing `Reset` while `GetDesc` reports the new size, so the extent is resolved from the device rather than snapshotted at creation; `GetDepthStencilSurface` likewise hands back one cached object; `GetDC` on the non-lockable default backbuffer returns `INVALIDCALL` and leaves the caller's out `HDC` slot as it found it. |
| `texture_stages.rs` | COLOROP round-trip; MODULATE/ADD/SELECTARG2; TFACTOR arg source; sampling a locked A16B16G16R16F texture. |
| `shaders.rs` | hand-assembled VS/PS; PS-constant colour; VS-constant translation; float-constant setters (in-range accept + out-of-range/`-1` → `INVALIDCALL`); integer/bool constant round trips; a `vs_2_0` static `if b0` branch driven by `SetVertexShaderConstantB` (TRUE and FALSE arms, no shader rebind); the same `if b0` in a `ps_3_0` driven by `SetPixelShaderConstantB`; a `ps_3_0` `rep i0` loop count driven by `SetPixelShaderConstantI` (4 iterations vs 0); a `ps_2_0` writing `oDepth` still drawing with no depth buffer bound (the depth export is dropped for that pass); `GetFunction` returning the creating bytecode (size query, round trip, undersized buffer, null size pointer) for both shader kinds and for a shader-model 1 program; a `ps_3_0` sampler reading the texture kind the application bound rather than the one its `dcl` names, over all four combinations of a 2D and a volume texture with a `dcl_2d` and a `dcl_volume` slot; a `ps_2_0` whose only `c0` read is `def`-declared keeping its literal colour across a `SetPixelShaderConstantF` upload; three draws in one pass alternating that program with one that does read `c0`, pinning that the reader still sees a row set between the draws; a `vs_3_0` whose only relative constant read (`c[a0.x + 0]`, the row index itself uploaded in `c0`) sits in a subroutine reached through `call`, drawing the colour uploaded twenty rows past anything the instruction stream names; a `vs_3_0` vertex texture fetch reading the texture bound at `D3DVERTEXTEXTURESAMPLER0` with `texldl`, and the same fetch declared `dcl_volume` over a 2D binding, pinning that the sampler argument and its coordinate follow the bound texture kind rather than the declaration. |
| `vertex_decl.rs` | `CreateVertexDeclaration` drives an FF draw; `GetVertexDeclaration` round-trip; a two-stream declaration through the FF pipeline; a declared stream with nothing bound reads zeros (bound and UP draws). |
| `transforms_ff.rs` | Set/Get/MultiplyTransform; FF diffuse passthrough; alpha test; Set/Get material + light + LightEnable; omitted declaration colors fall back to all four material sources, while declared colors on unbound streams read zero. |
| `render_target.rs` | Render-to-texture + sample; A16B16G16R16F render target create and draw/readback round trip; depth occlusion; auto depth-stencil Get/Set; `GetDepthStencilSurface` answering with the surface `SetDepthStencilSurface` bound (pointer identity against an app-created surface, a save/bind-other/restore leaving that surface bound, `D3DERR_NOTFOUND` and a null out-pointer once depth is unbound); CreateDepthStencilSurface; backbuffer desc; StretchRect 1:1 accept; a full-surface depth→depth StretchRect, its copy read back by depth-testing two quads against the destination and the filtered form accepted as well; StretchRect between two rects of one surface (a disjoint copy, and an overlapping shift whose result is the source as it stood before the copy); StretchRect format conversion (R5G6B5 into X8R8G8B8 through the scaling render quad, YUY2/UYVY decode into the backbuffer and, on the CPU path, into an offscreen-plain surface, including a source sub-rectangle converted into a destination level smaller than the source level); INTZ sampleable-depth dual-use (render-as-depth → sample) via both the FF and a programmable PS; a depth texture's content surviving a `CreateDepthStencilSurface` bind and a `Present` before it is sampled (the store action a texture-backed depth bind's `is_sampleable` keeps); the INTZ texture sampled while it is still the bound depth attachment (the encoder copies it first); `GetRenderTargetData` read-back into a SYSTEMMEM offscreen surface (`Surface::GetDevice` + `CreateOffscreenPlainSurface` + `LockRect`, pixels matched against the private export); `GetRenderTargetData` and `GetFrontBufferData` into level 0 of a `D3DPOOL_SYSTEMMEM` / `D3DPOOL_SCRATCH` texture, read back through `LockRect`, plus the destinations both reject (a GPU-resident level, another extent, another byte layout); a 33x4 R5G6B5 read-back pinning that the blit writes its rows at the pitch the destination's `LockRect` reports, not the tight one (an odd width in a 16-bit format is where the two differ); `StretchRect` into a lockable DEFAULT offscreen plain read back through `LockRect` (whole surface and a sub-rect that leaves the rest alone), with a `D3DLOCK_DISCARD` lock after such a blit keeping the pixels it wrote instead of the blitted ones, `StretchRect` from a render target into a render-target texture level read back through `GetDC`/`GetPixel` over staging a lock seeded with another colour, the same round trip through `LockRect` for the two copies that take the render quad instead of the 1:1 blit (a 64x64 render target scaled into a 32x32 render-target texture level, and a same-size R5G6B5 offscreen plain converted into an A8R8G8B8 one), the same blit into face 3 of a DEFAULT render-target cube read back through that face's `GetDC` with a differently filled face 0 left alone, a `ColorFill` of two cube faces each reading its own colour back through its own `GetDC`, `GetRenderTargetData` from a render-target cube's face-3 surface and from that face's level 1 reading each subresource's own fill while face 0 keeps its own, a scaling `StretchRect` into face 3 of a DEFAULT render-target cube sampled back along that face's direction with a differently filled face 0 left alone, a scaling `StretchRect` out of a cube face onto the back buffer carrying that face's colour, a 1:1 same-format `StretchRect` out of face 3 into a 2D render target carrying face 3's colour rather than face 0's, a 1:1 same-format `StretchRect` from face 1 onto face 3 of one cube at the same rectangle carrying face 1's colour (both endpoints resolve to one Metal texture, where equal rectangles on two faces are still a real copy), the scaling form of that pair landing the whole of face 1 in face 3's top-left quarter through the scratch detour and leaving the rest of face 3 alone, and the surface-class restriction that keeps a render-target source out of an offscreen-plain destination (standalone `CreateRenderTarget`, render-target texture level, and the reverse direction accepted); surface-op contracts (ColorFill/CreateRenderTarget` rejecting an unmappable format, `GetRenderTargetData`/`GetFrontBufferData` refusing a non-SYSTEMMEM destination, `CreateOffscreenPlainSurface` taking `D3DPOOL_DEFAULT` and refusiong `D3DPOOL_MANAGED`); a fresh DEFAULT offscreen plain served out of its own staging (a read-only lock before anything wrote it, a `LockRect` write and the `LockRect` read after it, and a `ColorFill` of a plain no lock has touched); `GetDC` on a DEFAULT offscreen plain reading the `ColorFill` before it, with GDI's `SetPixel` through the DC reaching both the staging a later `LockRect` maps and the Metal texture a 1:1 `StretchRect` into the back buffer reads; `ColorFill` of a sub-rect and of an over-the-edge rect on a lockable DEFAULT offscreen-plain surface, read back inside and outside through `LockRect`; `ColorFill` of a DEFAULT offscreen plain in each of A1R5G5B5, X1R5G5B5, A4R4G4B4, L8 and A8 read back through `LockRect` as that format's own packing of the fill colour, over a seed the fill has to replace; `ColorFill` of a bound render target erasing an earlier draw and blending under a later `SRCALPHA` one; `ColorFill` into level 0 of an `AUTOGENMIPMAP` render-target texture regenerating the chain (a sample pinned to level 4 through `D3DSAMP_MAXMIPLEVEL` reads the fill colour, not the seeded chain); a lockable `CreateRenderTarget` surface staying coherent with its CPU staging in every direction (a whole-surface and a sub-rect `ColorFill`, a `Clear` and a draw all read back through `LockRect`, and a `GetDC` that shows the GPU's pixels with GDI's own drawing reaching the colour texture at `ReleaseDC`, and the same `GetDC` round trip on a 33x4 R5G6B5 surface, whose odd row length puts the last row out of reach of a DIB over a tighter store); a lockable `CreateRenderTarget` surface created at the back buffer's own size keeping the extent D3D9 reports rather than inheriting `render.scale`, with the bind-and-`Clear`, the `ColorFill`, the `LockRect` read-back and the `UnlockRect` upload all probed on the far corner a scaled texture would not reach; a depth texture with a mip chain (per-level `GetLevelDesc`, level 1 bound as the depth attachment beside the back buffer and gating a farther draw); a rect-bounded depth `Clear` touching only its rects; a full-surface depth→depth `StretchRect` feeding a later depth test; `StretchRect` addressing an upper mip level on both the source and the destination, on the scaling and the 1:1 path; `Clear(D3DCLEAR_ZBUFFER)` rejected once no depth-stencil is bound and accepted for an offscreen target with a custom depth surface; `CreateRenderTarget` reporting a DEFAULT-pool `D3DUSAGE_RENDERTARGET` surface (against a DEFAULT offscreen-plain surface reporting no usage), accepting A32B32G32R32F and rejecting an unmappable format; a DEFAULT-pool render target bound, drawn into and read back; 1:1 `StretchRect` between two DEFAULT render targets, from a rendered target into the backbuffer across a `Present`, and into a backbuffer with a pending `Clear` (the copy lands last); `SetRenderTarget` snapping the viewport and scissor to the new target; a float-texture sample rendered into an A32B32G32R32F target; depth surviving a mid-frame readback flush (the far group still fails LESS against the primed near depth); the RESZ resolve (`D3DRS_POINTSIZE` = `0x7FA05000` with the destination INTZ texture at stage 0) copying the bound depth-stencil into that texture, read back by sampling the destination, and the `RESZ` pseudo-format probe answering `D3D_OK`; the `oDepth` depth restore, a `ps_3_0` writing sampled INTZ depth into the bound depth buffer and gating a later sprite z-test, with a colour-writing twin painting the sampled depth back as grayscale; INTZ carrying a working stencil plane (a REPLACE mark gating a later EQUAL draw); and, under `depth.aliasSameSize`, a same-size depth-stencil rebind whose z-test sees the contents rendered through the other surface; `GetDC` on a DEFAULT offscreen plain rejected while its own `LockRect` is outstanding, and accepted once it is released; 64 rounds of create, bind, depth-test and release of a standalone `CreateDepthStencilSurface` surface, one per frame, each round's depth test proving its own fresh texture and the run proving the retirement of the previous one lands after the frame that drew with it.` on the standalone backbuffer, ``; a non-lockable A16B16G16R16F render target locked read-only reporting the pitch its own format asks for (eight bytes per texel), with the last texel of the last row inside the staging; `LockRect` and `UnlockRect` on a non-lockable `CreateRenderTarget` surface both rejected with the caller's `D3DLOCKED_RECT` left untouched, and the same surface created lockable lockinge |
| `render_scale.rs` | Every assertion in *reported* coordinates, so the whole file must pass unchanged under `make test SCALE=0.75`: the scissor confining a whole-target clear; `Clear` with `pRects`, and those rects intersected with the scissor; the viewport bounding a whole-target colour clear, and bounding the colour plane of a combined `TARGET | ZBUFFER` clear; a whole-target depth clear bounded to the viewport, read back through the depth test, including that it writes `Clear`'s raw Z rather than one remapped through the viewport's `MinZ`; rebinding the back buffer by surface keeping its coordinate space; a back-buffer readback carrying the frame's alpha rather than an opaque one; a game-created render target passing through untouched; a resizing `Reset` reporting and clipping in the new size; `ColorFill` of a sub-rect of a render-target texture created at the reported back-buffer size, and of a sub-rect of that target's mip level 1 read back by sampling the level; `ColorFill` of a sub-rect of a standalone `CreateRenderTarget` surface at that size, copied onto the back buffer with `StretchRect`; and a point's diameter, which D3D9 states in the reported space and Metal consumes in the rasterized one, keeping its reported extent; a `D3DUSAGE_DEPTHSTENCIL` texture created at the reported back-buffer size charged the mip chain its Metal levels hold rather than the one its descriptor reports, while a texture of another size keeps costing what it asked for; and a `CreateRenderTarget` and a `CreateDepthStencilSurface` surface at that size each charged the texture their Metal create holds and refunded it on release, while a target of another size keeps costing what it asked for (those three tests pin `render.scale=0.5` themselves, since none of those quantities is observable at the identity). |
| `implicit_surface.rs` | The device-owned implicit surfaces: `GetRenderTarget(0)` and `GetBackBuffer(0)` handing back one cached object that survives its refcount reaching zero and resolves its extent live across a resizing `Reset`; the cached implicit depth-stencil; `GetDC` on a non-lockable back buffer rejected with the caller's out `HDC` untouched, and on a `D3DPRESENTFLAG_LOCKABLE_BACKBUFFER` one reading the pixels the GPU painted and carrying GDI's own drawing back into the back buffer at `ReleaseDC`, with no `Present` in between, both at the extent D3D9 reports and, with `render.scale` pinned below 1.0 in the test's own process, through the resample the write-back needs when the back buffer is rasterized smaller. |
| `msaa.rs` | Multisampling: `CheckDeviceMultiSampleType` (NONE and 4x on a colour and a depth format, NONMASKABLE reporting its quality-level ladder; an in-enum count no hardware offers and `D3DFMT_UNKNOWN` separating NOTAVAILABLE from INVALIDCALL, multisampled INTZ and DXT1 as NOTAVAILABLE while the single-sampled probe still passes); `CreateRenderTarget` / `CreateDepthStencilSurface` honouring the type and reporting it back through `GetDesc`, with a lockable multisampled target and an unserveable count rejected; the resolve, counted as intermediate pixels along a diagonal edge that a single-sampled target has none of, on a standalone render target resolved with `StretchRect` and read through `GetRenderTargetData`, on a multisampled swap chain read through Present, and through a `StretchRect` out of a multisampled source, with a copy back in and out again coming through unchanged; a depth-tested draw on a 4x target, and a `CreateDepthStencilSurface`-made 4x depth surface bound beside a 4x back buffer; `D3DRS_MULTISAMPLEMASK` at 0, at half the samples, and back at its default; a single-sampled depth surface left bound under a multisampled target, whose draw still reaches the target once the pass drops the depth attachment; the RESZ hack out of a 4x depth surface into an INTZ texture, sampled back and matched against the same scene's single-sampled RESZ; a depth-to-depth `StretchRect` out of a 4x depth surface, observed through a draw the resolved depth rejects; a `Clear` of a render target ordered before a `StretchRect` resolve into it, with a draw over one corner afterwards, leaving the resolved image everywhere that draw does not cover. |
| `mrt.rs` | Multiple render targets: `ps_3_0` writing `oC0`/`oC1` into two bound targets; `Clear` reaching every bound target and an unwritten target keeping its contents; `D3DRS_COLORWRITEENABLE1` masking slot 1 alone; the slot contract (four slots, slot 0 never null, `NOTFOUND` on an unbound slot, `SetRenderTarget(1, NULL)`); `Reset` unbinding slots 1..3; a target sized unlike slot 0 cleared but left out of draws; a mid-pass `Clear` and a rect `Clear` reaching both targets through the in-pass quad; the MRT caps bits; a clear-only pass held ahead of an MRT pass that draws into the cleared target at slot 1. |
| `state_block.rs` | Capture/Apply (ALL); VERTEXSTATE restores FVF; PIXELSTATE restores sampler; Begin/EndStateBlock recording, and a second `BeginStateBlock` rejected with the open recording left intact. |
| `query.rs` | EVENT fence; OCCLUSION sample count; TIMESTAMP contract. |
| `resource_misc.rs` | Factory refcount; `QueryInterface` identity on the device and the factory (`IUnknown` + own interface hand back the same object one reference stronger, anything else → E_NOINTERFACE, refcount untouched); `GetType` on a texture and on one of its levels; no-op PreLoad/SetPriority; `GetAvailableTextureMem` (non-zero, and the reported figure dropping by exactly a standalone `CreateRenderTarget` and a `CreateDepthStencilSurface` surface's bytes while each is alive, back to where it started once both are released, and likewise by a `D3DUSAGE_DEPTHSTENCIL` texture's own bytes for as long as it is held; a 4x `CreateRenderTarget` surface dropping it by five times its single-sample bytes, the resolve target plus the multisampled companion, and a 4x `CreateDepthStencilSurface` surface by four, its one multisampled attachment; an odd-width `D3DFMT_D16` mip chain costing exactly what the `D3DFMT_R5G6B5` chain of the same shape costs, and an odd-width `D3DFMT_D16` `CreateDepthStencilSurface` surface costing exactly what the one-level depth texture of the same size costs, all measured on the host-visible row pitch); `EvictManagedResources`; `GetDevice` answering with the creating device from every wrapper it was a stub on (both buffers, the 2D, cube and volume textures in the pools that do and do not pin the device, a volume level, a texture's own surface and a standalone render target, both shaders, the declaration, the state block and the query), each releasing the reference it was handed and checking the device count back to its pre-call value; a `D3DPOOL_SCRATCH` cube and a `D3DPOOL_SYSTEMMEM` texture each answering `GetDevice` with the live device after the application released its own device reference, and taking the device down when they go; a null `this` and a null out-pointer answered rather than faulted; the private-data round trip (store, size query, read back, free) on both buffer kinds, the `IUnknown` form's reference lifecycle, and `D3DERR_MOREDATA` on an undersized read; `SetClipPlane`/`GetClipPlane` round trip; `ValidateDevice` → S_OK (single-pass valid); `SetGammaRamp` no-op. |
| `d3dperf.rs` | The seven `D3DPERF_*` exports resolved by `GetProcAddress` the way an engine's d3d9 loader does (no harness, so a missing export is a test failure rather than a link error), and the no-profiler contract: `BeginEvent`/`EndEvent` report no nesting, `QueryRepeatFrame` is `FALSE`, `GetStatus` is `0`, the marker and option setters are no-ops. An in-game overlay SDK refuses to initialise when any is missing. |
| `unload.rs` | `LoadLibrary` → `Direct3DCreate9` → `Release` → `FreeLibrary`, then a continuable exception raised with a resuming handler appended at the end of the chain: proves the unloaded image left no vectored exception handler behind (no harness: its `raw-dylib` import would keep the module mapped). |
| `snmalloc_drift.rs` | Not a Direct3D test (no harness, no `d3d9.dll`): the PE half of the guard on `page_box`'s model of snmalloc, whose constants are read out of vendored C++ and never cross the C ABI. The binary installs snmalloc as its own global allocator and pins the observable signature of the per-thread large-object cache budget, the decommit: a 4 MiB allocation, past the budget per `bypasses_local_cache`, reads `MEM_COMMIT` through `VirtualQuery` while live and `MEM_RESERVE` once freed, so an upgrade that moves `LocalCacheSizeBits` fails here instead of leaving `SNMALLOC_LOCAL_CACHE_BYTES` and the uncached-allocation counter built on it quietly wrong. Only the Windows PAL returns pages, so the host-native `mtld3d-core/tests/snmalloc_drift.rs` covers the power-of-two rounding half alone. |

## Documented limitations / stubs pinned by tests

These return `D3DERR_INVALIDCALL` (or are no-ops) by design — the target
workload does not need them, or Metal cannot represent them. Tests pin the
contract so a future implementation flips a known assertion.

- **Draw:** `ProcessVertices` from an explicit vertex declaration.
- **Textures:** ATI1 and YUV cubes are CPU-only `D3DPOOL_SCRATCH` resources;
  GPU-backed cubes support mapped color and DXT formats. `SetLOD` is a
  managed-pool-only no-op. `UpdateSurface` / `UpdateTexture` convert a
  mismatched format pair on the CPU for the uncompressed colour formats whose
  channels are unsigned normalised and at most 8 bits wide (A8R8G8B8,
  X8R8G8B8, A8B8G8R8, X8B8G8R8, R8G8B8, R5G6B5, A1R5G5B5, X1R5G5B5, A4R4G4B4,
  L8, A8, A8L8, plus the packed YUV pair as a source) and reject any other
  mismatch, where D3D9 converts.
- **Samplers:** arbitrary `D3DSAMP_BORDERCOLOR` (Metal has 3 preset borders).
- **Shaders:** pixel-shader integer and boolean constants are stored only (a `defi`/`defb` local or a VS dynamic `iN`/`bN` is consumed); `Get{Vertex,Pixel}ShaderConstantF`.
- **Multisampling:** `D3DRS_MULTISAMPLEANTIALIAS` is a no-op (Metal has no
  per-draw multisample toggle); `D3DPRASTERCAPS_MULTISAMPLE_TOGGLE` is not
  advertised to match. A depth attachment whose sample count differs from
  render target 0's is dropped for the pass, since Metal rejects a pass whose
  attachments disagree.
- **Surfaces:** `CreateOffscreenPlainSurface` is implemented for
  `D3DPOOL_SYSTEMMEM` only (DEFAULT/MANAGED rejected); `GetRenderTargetData` /
  `GetFrontBufferData` read a backbuffer, a standalone-color RT or a
  render-target texture level into any system-memory destination, and reject a
  destination whose extent or byte layout differs from the source.
- **Resources:** `ValidateDevice` returns S_OK (single-pass valid);
  `SetGammaRamp` is a no-op. Two `INVALIDCALL`s here are deliberate
  divergences rather than gaps, both where D3D9 itself leaves no room to
  fail: `GetDevice` answers `INVALIDCALL` once the creating device is gone,
  which only the resources that do not pin it can reach (a MANAGED texture,
  the CPU-only cube shell) and only after an application released a device
  it still holds resources from; `GetFunction` answers `INVALIDCALL` for a
  null `pSizeOfData`, the size slot being the call's only channel in both
  directions, so there is nothing to answer without one.
- **Legacy:** `SetPaletteEntries`, `GetRasterStatus`, `GetClipStatus`,
  `SetDialogBoxMode`.

INTZ sampleable-depth (cascade shadows): covered synthetically — the depth
texture is rendered into, then sampled in a later pass (depth unbound first so it
is not a live attachment), through both the fixed-function pixel pipeline and a
hand-assembled programmable PS. A `Depth32Float` slot emits `depth2d<float>` +
`sample_compare`; `make test` runs with Metal validation on, so a
depth/`texture2d` or attachment/format mismatch fails the test.
